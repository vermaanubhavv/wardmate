# Copy to  env/deploy.env  (git-ignored, never sent anywhere).
# `npm run deploy:init` creates this for you — you normally don't edit it by hand.

# The only value that can't be discovered automatically.
# Supabase -> Project Settings -> Database -> Connection string -> URI (direct, port 5432)
SUPABASE_DB_URL=postgresql://postgres:PASSWORD@db.zrisashumxmiiwffhezc.supabase.co:5432/postgres

# Locally these are read from your `vercel login` + `vercel link` automatically.
# Only set them by hand if that autodiscovery fails.
# VERCEL_TOKEN=
# VERCEL_PROJECT_ID=
# VERCEL_TEAM_ID=
